# Desktop installs both tiers into one environment (one process runs both
# today — see CLAUDE.md "Deployment reality"). Split per-tier files live in
# requirements/{shared,cloud,agent}.txt; the VPS Docker image installs
# requirements/cloud.txt directly instead of this shim.
-r requirements/cloud.txt
-r requirements/agent.txt
